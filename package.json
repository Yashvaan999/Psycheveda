{
  "name": "psycheveda",
  "version": "0.1.0",
  "private": true,
  "description": "Psycheveda monorepo — production app lives in mobile/",
  "scripts": {
    "dev": "npm run web --prefix mobile",
    "web": "npm run web --prefix mobile",
    "build": "npm run build --prefix mobile",
    "preview": "npm run preview --prefix mobile",
    "clean": "rm -rf mobile/dist",
    "rebuild": "npm run clean && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
